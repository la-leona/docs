# Markdown 치트시트

기준: **GitHub Flavored Markdown (GFM)** — GitHub/Bitbucket PR·이슈, VS Code, IntelliJ 미리보기에서 대부분 동일하게 동작.

---

## 1. 제목 (Heading)

`#` 개수가 제목 단계가 된다. (이 문서의 `# Markdown 치트시트` = H1, `## 1. 제목` = H2)

```text
# 제목 1   (H1)
## 제목 2  (H2)
### 제목 3 (H3)
#### 제목 4 (H4)
```

- `#` 뒤에 **공백 필수** — `#제목` 은 제목이 되지 않는다.
- 문서당 H1(`#`)은 1개 권장, 본문은 `##` 부터 시작.
- **표 셀 안에서는 제목이 렌더링되지 않는다** (아래 "표 안에서 안 되는 것" 참고).

## 2. 강조

| 문법 | 결과 |
|--|--|
| `**굵게**` | **굵게** |
| `*기울임*` | *기울임* |
| `***굵은 기울임***` | ***굵은 기울임*** |
| `~~취소선~~` | ~~취소선~~ |
| `` `인라인 코드` `` | `인라인 코드` |

- 한글은 `_기울임_` 이 단어 중간에서 안 먹을 수 있으니 **`*` 사용 권장**.

## 3. 목록

소스:

```text
- 순서 없는 목록 (`-` 뒤 공백 필수)
- 두 번째
  - 하위 (공백 2칸 들여쓰기)

1. 순서 있는 목록
2. 두 번째
```

결과:

- 순서 없는 목록
- 두 번째
  - 하위 (공백 2칸 들여쓰기)

1. 순서 있는 목록
2. 두 번째

- 순서 있는 목록은 전부 `1.` 로 써도 자동 증가 → 항목 추가·삭제가 편하다.
- 목록 항목 안에 문단이나 코드블록을 넣으려면 **2칸 들여쓰기를 유지**한다.
- 목록 **위쪽에 빈 줄**이 없으면 렌더링이 안 될 수 있다.

## 4. 체크박스 (작업 목록)

`- [ ] 할 일` / `- [x] 완료된 일` → 아래처럼 보인다.

- [ ] 할 일
- [x] 완료된 일

## 5. 코드

**인라인 코드** — 백틱 1개로 감싼다.

| 문법 | 결과 |
|--|--|
| `` `getMemCi()` `` | `getMemCi()` |
| ``` `` `백틱` `` ``` | `` `백틱` `` |

- **백틱을 그대로 보여주려면 감싸는 백틱을 더 많이** 쓴다: 안에 백틱 1개면 2개로, 안에 2개 연속이면 3개로 감싼다.

**코드블록** — 백틱 3개 + 언어 태그. 아래가 소스이고,

````text
```java
public void main() {
    log.debug("hello");
}
```
````

그 결과는 이렇게 보인다.

```java
public void main() {
    log.debug("hello");
}
```

자주 쓰는 언어 태그: `java` `sql` `xml` `json` `yaml` `js` `ts` `html` `css` `bash` `powershell` `python` `diff` `text`

**변경 설명에는 `diff` 가 효과적** (`+` 초록 / `-` 빨강). 소스:

````text
```diff
- String memCi = dto.getMemCi();
+ String memCi = ciEncHelper.ciEncOn() ? dto.getMemEncCi() : dto.getMemCi();
```
````

결과:

```diff
- String memCi = dto.getMemCi();
+ String memCi = ciEncHelper.ciEncOn() ? dto.getMemEncCi() : dto.getMemCi();
```

**코드블록 안에 코드블록**을 보여줄 때는 **바깥을 백틱 4개**로 감싼다:

````text
```java
안쪽 블록
```
````

## 6. 표 (Table)

기본형 — 헤더 바로 아래 **하이픈 구분행이 필수**다. 소스:

```text
| 항목 | 값 | 설명 |
|--|--|--|
| A | 1 | 첫째 |
| B | 2 | 둘째 |
```

결과:

| 항목 | 값 | 설명 |
|--|--|--|
| A | 1 | 첫째 |
| B | 2 | 둘째 |

정렬은 구분행에 콜론을 넣는다 (`:--` 왼쪽 / `:--:` 가운데 / `--:` 오른쪽). 소스:

```text
| 왼쪽 | 가운데 | 오른쪽 |
|:--|:--:|--:|
| L | C | R |
```

결과:

| 왼쪽 | 가운데 | 오른쪽 |
|:--|:--:|--:|
| L | C | R |

- 소스의 세로선 위치가 안 맞아도 **결과는 정상** (정렬은 가독성용).
- 셀 안 줄바꿈은 `<br>` 사용.
- 셀 안에 세로선 문자를 쓰려면 `\|` 로 이스케이프 (인라인 코드 안이라도 필요).
- 표 앞뒤에 **빈 줄**을 넣어야 안전하다.

### 표 안에서 안 되는 것 (블록 요소)

셀에는 **인라인 요소만** 들어간다. 다음은 표 안에서 렌더링되지 않으니 표 밖에서 예시를 보여야 한다.

| 안 되는 것 | 대안 |
|--|--|
| 제목 (`# ##`) | 표 밖에서 실제 제목으로 쓰거나 코드블록으로 문법만 표시 |
| 목록 (`-`, `1.`), 체크박스 (`- [ ]`) | 셀에서는 `<br>` 로 줄만 나눔 / 목록은 표 밖에 |
| 코드블록 (백틱 3개) | 셀에는 인라인 코드(`` ` ``)만, 블록은 표 밖에 |
| 인용 (`>`), 구분선 (`---`) | 표 밖에서 사용 |

셀에서 되는 것: **굵게**, *기울임*, ~~취소선~~, `인라인 코드`, 링크, 이미지, `<br>`, `<kbd>` 등.

## 7. 링크 / 이미지

| 문법 | 설명 |
|--|--|
| `[텍스트](https://example.com)` | 기본 링크 |
| `[텍스트](https://example.com "툴팁")` | 툴팁 포함 |
| `[문서](./다른파일.md)` | 같은 폴더의 파일 (로컬 md 링크) |
| `[섹션](#6-표-table)` | 문서 내 앵커 — 헤딩을 소문자로, 공백은 `-` |
| `<https://example.com>` | 자동 링크 |
| `![대체텍스트](./img/shot.png)` | 이미지 (앞에 `!`) |

- 한글 헤딩 앵커는 `#소제목-이름` 처럼 공백만 `-` 로 바꾸면 대부분 동작한다.

## 8. 인용 / 구분선

소스:

```text
> 인용문
> 여러 줄 가능
>> 중첩 인용

---
```

결과:

> 인용문
> 여러 줄 가능
>> 중첩 인용

---

- `---` 를 한 줄에 단독으로 쓰면 구분선(가로줄)이 된다. (위의 가로줄이 그 결과)
- 단, **제목 바로 아래**에 `---` 를 쓰면 구분선이 아니라 그 윗줄을 제목으로 만드는 문법이 될 수 있으니 앞에 빈 줄을 둔다.

## 9. 줄바꿈 (가장 흔한 함정)

| 원하는 것 | 방법 |
|--|--|
| 문단 나누기 | **빈 줄** 하나 |
| 같은 문단에서 줄만 바꾸기 | 줄 끝에 **공백 2개**, 또는 `<br>` |
| 엔터 1번만 누름 | 렌더링에서 **한 줄로 붙어버림** (주의) |

## 10. 접기 / 펼치기 (HTML 혼용)

<details>
<summary>클릭해서 펼치기</summary>

안에서도 마크다운을 쓸 수 있다 (앞뒤로 빈 줄 필요).

</details>

위 블록의 소스:

````html
<details>
<summary>클릭해서 펼치기</summary>

안에서도 마크다운을 쓸 수 있다 (앞뒤로 빈 줄 필요).

</details>
````

- GFM에서 허용되는 태그 일부: `<br>` `<details>` `<summary>` `<kbd>` `<sub>` `<sup>` `<img>`
- `<kbd>Ctrl</kbd>+<kbd>C</kbd>` → <kbd>Ctrl</kbd>+<kbd>C</kbd>

## 11. 이스케이프 (기호를 그대로 쓰기)

기호 앞에 역슬래시(`\`)를 붙인다.

| 쓰고 싶은 기호 | 입력 |
|--|--|
| 별표 | `\*` |
| 밑줄 | `\_` |
| 샵 | `\#` |
| 세로선 | `\|` |
| 대괄호 | `\[` `\]` |
| 역슬래시 | `\\` |
| 백틱 | 역슬래시가 아니라 **더 많은 백틱으로 감싸기** (5장 참고) |

예) `\*강조 아님\*` → \*강조 아님\*

## 12. 각주 (GFM)

`본문에 각주[^1] 표시.` 그리고 문서 아래쪽에 `[^1]: 각주 내용.` 을 적는다.

---

## 자주 겪는 함정 정리

| 증상 | 원인 / 해결 |
|--|--|
| 줄바꿈이 안 됨 | 엔터 1번은 무시됨 → **빈 줄** 또는 **줄 끝 공백 2개** |
| 목록이 렌더링 안 됨 | `-` 뒤 **공백** 필요, 목록 **위에 빈 줄** 필요 |
| 표가 깨짐 | 헤더 아래 **하이픈 구분행 필수**, 표 앞뒤 **빈 줄** |
| 표 셀이 밀림 | 셀 안 세로선을 `\|` 로 이스케이프하지 않음 |
| 표 셀의 제목·목록·코드블록이 안 먹음 | 셀에는 **인라인 요소만** 가능 → 블록 요소는 표 밖으로 (6장 참고) |
| 백틱을 보여주려는데 깨짐 | 안쪽 백틱 연속 개수보다 **감싸는 백틱을 1개 더** 많게 (2개 안이면 3개로 감싸기) |
| 제목이 텍스트로 보임 | `#` 뒤 **공백** 누락 |
| **문서 뒷부분이 전부 코드블록** | 백틱 3개 블록 안에 또 백틱 3개를 넣어 **펜스가 깨짐** → 바깥을 **백틱 4개**로 |
| 들여쓴 글이 코드블록이 됨 | 공백 4칸 들여쓰기는 코드블록으로 해석됨 |
| 원문자(①②)가 작게 보임 | md 뷰어에서 가독성 나쁨 → `(1) (2)` 나 일반 번호 사용 |

## 문서 작성 권장 패턴 (실무)

1. 상단에 메타 정보(작성일·담당·이슈번호)를 불릿으로.
2. **결론을 먼저** — 요약 표 하나로 스캔되게.
3. 상세는 표 + 코드블록 조합.
4. 마지막에 **남은 일 / 후속**.

- 파일·경로·메서드명은 백틱으로: `LoginJspController.java:5535`
- 소스 변경 설명은 `diff` 코드블록이 가장 잘 읽힌다.
- 표는 3~5열이 적당. 그 이상은 항목별 소제목 + 불릿이 낫다.
