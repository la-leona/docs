# 로그 글자 색깔 출력 방법 (log.debug 등)

> slf4j `log.debug/info/...` 로그를 콘솔에서 색상(노랑/빨강 등)으로 강조하는 방법.
> ANSI 이스케이프 문자는 Java 소스에서 `\u001B` 로 표기한다(= ESC 제어문자, 8진 `\033`).

---

## 방법 1 — 메시지에 ANSI 코드 직접 삽입 (가장 간단)

```java
// 노랑
log.debug("\u001B[33m### CI={}, MOBILE={} ###\u001B[0m", ci, mobile);
// 빨강
log.debug("\u001B[31m### 에러 지점 값={} ###\u001B[0m", val);
```

- `\u001B[33m` = 색 시작(노랑), `\u001B[0m` = **리셋**(반드시 끝에 붙여야 이후 로그가 안 물듦).

상수로 빼면 깔끔:

```java
private static final String C_RESET  = "\u001B[0m";
private static final String C_RED    = "\u001B[31m";
private static final String C_YELLOW = "\u001B[33m";
private static final String C_CYAN   = "\u001B[36m";

log.debug("{}### CI={} MOBILE={}{}", C_YELLOW, sConnInfo, sMobileNo, C_RESET);
```

---

## 주요 색상 코드 (`\u001B[` + 아래 숫자 + `m`)

| 색 | 숫자 | | 색 | 숫자 |
|---|---|---|---|---|
| 빨강 | `31` | | 파랑 | `34` |
| 초록 | `32` | | 자홍 | `35` |
| 노랑 | `33` | | 청록 | `36` |
| 굵게 | `1` | | 리셋 | `0` |

- 예: 노랑 = `\u001B[33m`, 리셋 = `\u001B[0m`
- 배경색: 40번대 (예: `\u001B[43m` = 노랑 배경)
- 조합: `\u001B[1;31m` = 굵은 빨강

---

## 주의점 (중요)

1. **콘솔에서만 색이 보인다.** IntelliJ 콘솔·터미널은 ANSI 렌더링하지만, **파일 appender로 나가는 로그에는 `\u001B[33m` 에 해당하는 raw 제어문자가 그대로 박힘**(로그파일 오염). 로컬 디버깅용으로만, 커밋 전 제거 권장.
2. IntelliJ에서 색이 안 나오면:
   - 실행 구성 VM 옵션에 `-Dspring.output.ansi.enabled=ALWAYS` 추가, 또는
   - `application-local.yml` 에:
     ```yaml
     spring:
       output:
         ansi:
           enabled: always
     ```
   - (이 설정은 Spring Boot 기본 패턴의 색상 렌더링 강제용. 메시지에 직접 넣은 ANSI 코드는 대체로 그와 무관하게 콘솔이 지원하면 표시됨.)

---

## 방법 2 — 로그 "레벨별" 색 (참고)

Spring Boot 기본 콘솔 패턴은 `%clr(...)` 컨버터로 **레벨(ERROR=빨강 등)** 에 자동 색상을 입힌다. 단 이건 레벨 기준 자동 색상이므로, "이 특정 로그만 노랗게" 하려면 방법 1(메시지에 직접 삽입)을 써야 한다.

---

## 개인정보 주의
- CI·DI·휴대폰번호 등 민감정보를 색으로 강조해 찍을 때도, 원문(raw) 로그는 위험.
- 마스킹 + 임시 사용, 확인 후 제거, 커밋 미포함 원칙.
