# 개념 정리 — 쿠키 / 세션 / 토큰 인증 비교

- 작성일: 2026-07-31
- 목적: **쿠키·세션·토큰**의 관계와 차이를 이해하고, lgoneid 가 **각각을 어디에 쓰는지** 파악.
- 관련 문서: `개념정리_Redis_Kafka_OAuth_DB라우팅.md` (Redis·Kafka·OAuth·DB 읽기/쓰기 라우팅)

---

# 0. 먼저 정리 — 쿠키는 "인증 방식"이 아니다

가장 많이 혼동하는 지점부터 짚는다.

> **쿠키 = 브라우저가 값을 저장하고 매 요청에 자동으로 실어 보내는 "그릇/전달 수단"**
> **세션·토큰 = 그 그릇에 무엇을 담고, 서버가 그것을 어떻게 신뢰할지 정하는 "방식"**

그래서 정확히는 이렇게 나뉜다.

| 흔한 표현 | 실제 의미 |
|--|--|
| "쿠키 인증" | 대개 (a) **쿠키에 인증 정보를 직접 담는** 옛 방식, 또는 (b) 쿠키로 세션ID를 주고받는 **세션 인증**을 뜻함 |
| "세션 인증" | 서버가 상태를 기억하고, **쿠키에는 세션ID만** 담음 |
| "토큰 인증" | 토큰 자체에 정보/만료가 있고, 보통 **헤더(`Authorization`)** 로 보냄 |

**즉 세션 인증도 쿠키를 쓴다.** 대립 관계가 아니라, "쿠키에 **무엇을** 담느냐"의 차이다.
```
쿠키에 세션ID만        → 세션 인증   (서버가 기억)
쿠키에 사용자 정보 직접 → 쿠키 인증   (서버가 안 기억, 위조 위험 → 서명 필요)
헤더에 토큰            → 토큰 인증   (모바일·외부 서비스에 적합)
```

---

# 1. 핵심 차이 한 줄

| | 쿠키(직접 담기) | 세션 인증 | 토큰 인증 |
|--|--|--|--|
| 상태 저장 위치 | **클라이언트(쿠키 값)** | **서버**가 기억 | **토큰 자체** |
| 클라이언트가 들고 있는 것 | 값 그대로(또는 서명된 값) | 세션 ID (의미 없는 난수) | access token (내용·만료 포함) |
| 전송 방법 | 쿠키 헤더(자동) | 쿠키 헤더(자동) | `Authorization: Bearer ...` (직접) |

비유:
- **쿠키(직접 담기)** = 내 손에 든 **메모지**("나 성인임"). 위조하기 쉬워서, 서버가 **도장(서명)** 을 찍어두지 않으면 신뢰할 수 없다.
- **세션** = 놀이공원 **보관함 키**. 키에는 아무 정보 없고, 내 물건은 보관함(서버)에 있다. → 보관함이 사라지면(서버 재시작·세션 만료) 끝.
- **토큰** = **입장 팔찌**. 팔찌 자체에 "성인, 오늘 23시까지 유효"가 적혀 있다. → 직원이 명부를 안 봐도 확인 가능. 단 **팔찌를 뺏기면** 그 사람이 나처럼 행동할 수 있다.

---

# 2. 쿠키 (그릇) — 속성과 우리 프로젝트의 쿠키들

## 알아둘 속성 (보안과 직결)
| 속성 | 뜻 | 왜 중요한가 |
|--|--|--|
| **HttpOnly** | JS(`document.cookie`)로 못 읽음 | XSS 로 세션 탈취되는 것을 막는다 |
| **Secure** | HTTPS 에서만 전송 | 평문 구간 노출 방지 |
| **SameSite** | 다른 사이트 요청에 쿠키를 붙일지 | CSRF 방어와 관련 |
| **Max-Age / Expires** | 만료 시점. **`-1`(미지정) = 세션 쿠키** | 세션 쿠키는 **브라우저를 닫으면 삭제** |
| **Domain / Path** | 어느 도메인·경로에 보낼지 | 필요 이상으로 넓히면 위험 |

## lgoneid 의 세션 쿠키 설정
`user-web-service/src/main/resources/application.yml`
```yaml
    session:
      timeout: 1200s        # 20분 무동작 시 세션 만료(서버측)
      cookie:
        http-only: true     # JS 접근 차단
        max-age: -1         # 세션 쿠키 → 브라우저 종료 시 삭제
```
→ **두 개의 만료가 함께 작동한다**: 쿠키는 *브라우저 종료 시*, 서버 세션은 *20분 무동작 시*.

## 우리가 실제로 쓰는 쿠키
| 쿠키 | 용도 | 인증 관련 |
|--|--|--|
| 세션 쿠키(Spring Session) | 세션ID 전달 → 로그인 상태 식별 | **핵심** |
| `auto-login`, `auto-login-chk` | 자동로그인 **선택 여부 플래그** (앱 위주) | 보조 |
| `SESSION_S_UUID`(sUuid) | 단말/사용자 식별값. 자동로그인·기기 인증 흐름에서 사용 (`LoginJspController` ~1648) | 보조 |
| `lang` | 언어 설정 | 무관 |
| `_ga` | Google Analytics | 무관 |
| `reward-pop`, `_emergencyNotice` | 팝업 노출 제어 | 무관 |

- 주의: `auto-login` 은 "자동로그인 하겠다는 표시"이고, **그 쿠키만으로 로그인이 되는 게 아니다.** 실제 자동로그인은 `sUuid`·기기 인증 정보와 서버 검증(`AutoLoginHandlerImpl`)을 거친다. → **인증 판단을 클라이언트 쿠키 값에만 의존하지 않는 것**이 원칙이다.

## "쿠키에 정보 직접 담기"를 왜 잘 안 쓰나
```
Set-Cookie: userId=MEM00000035804; isAdmin=true      ← 절대 이렇게 하면 안 됨
```
사용자가 값을 **바꿔 보낼 수 있기 때문**이다. 그래서 쿠키에 정보를 담으려면 **서버 서명(HMAC)** 이 필수이고, 그 서명된 형태가 사실상 **토큰(JWT)** 이다. 결국 "안전한 쿠키 인증"은 **토큰을 쿠키에 담은 것**과 같아진다.

## 그런데 서명도 결국 텍스트인데, 위조 안 되나?

**안 된다. 비밀은 텍스트가 아니라 "키"에 있다.**

서명은 **데이터 + 서버만 아는 비밀키를 함께 넣고 돌린 해시값**이다(HMAC).
```
서명 = HMAC-SHA256( 비밀키, "userId=hong&isAdmin=false" ) = "a3f9c1..."   ← 이 텍스트가 붙는다
```
공격자가 값을 고쳐도:
```
변조:  isAdmin=true  + 서명 a3f9c1...(그대로 붙임)
검증:  서버가 HMAC(비밀키, "isAdmin=true") 계산 → b7e204...  ≠  a3f9c1...  → 거부
```
`isAdmin=true` 에 맞는 **새 서명을 만들려면 비밀키가 필요**한데 그게 없다. HMAC 은 **한 방향**이라 서명을 보고 키를 되짚을 수도 없다.

> **비유**: 밀랍 봉인. 편지 내용과 봉인 모양은 다 보이지만, **도장(비밀키)** 이 없으면 내용을 고친 뒤 같은 봉인을 다시 만들 수 없다. **"보인다 ≠ 만들 수 있다"**.

### 서명이 막아주는 것 / 못 막는 것
| 항목 | 서명으로 막히나 | 대응 |
|--|--|--|
| 내용 **변조** | ✅ 막힘(검증 실패) | — |
| 내용 **숨김** | ❌ JWT payload 는 base64 라 **누구나 읽음** | 비밀정보(비밀번호·CI 등)를 담지 말 것 |
| **탈취 후 그대로 사용**(재생) | ❌ 훔친 토큰은 유효 | **짧은 만료**, `HttpOnly`, HTTPS |
| 서버 **비밀키 유출** | ❌ 전부 무력화 | 키 관리·주기적 교체 |

### lgoneid 에서
- `JwtServiceImpl` 는 **HS256(HMAC-SHA256) + 비밀키** 로 서명한다.
  ```java
  .signWith(SignatureAlgorithm.HS256, this.generateKey())   // ~75
  ```
  그리고 **유효시간 10초** — 서명으로 위조를 막고, 짧은 수명으로 탈취 피해를 제한한다(짧게 전달하는 용도라 충분).
- **세션 ID 는 서명 방식이 아니라 "추측 불가능한 난수"** 전략이다. 값에 정보가 없으니 **위조할 대상 자체가 없고**, 서버가 Redis 에서 대조한다. 접근은 다르지만 목적은 같다.
- **`auto-login` 쿠키는 서명조차 없는 단순 플래그**다. 그래서 그것만으로 인증하지 않고 `sUuid`·기기 인증 + 서버 검증을 거친다 → **"서명 없는 클라이언트 값은 신뢰하지 않는다"** 는 원칙의 적용.

---

# 3. 세션 인증 (우리 user-web 로그인 방식)

## 흐름
```
(1) 로그인 성공
(2) 서버가 세션 생성 → 세션 저장소에 {세션ID: 사용자정보} 저장
(3) 브라우저에 세션ID를 쿠키로 내려줌 (JSESSIONID 등)
(4) 이후 요청마다 쿠키의 세션ID로 서버가 "누구인지" 조회
(5) 일정 시간 요청이 없으면 만료 (우리는 1200초 = 20분)
```

## lgoneid 에서
- **Spring Session + Redis** — `user-web-service/.../common/config/SessionConfig.java`
  ```java
  @EnableRedisHttpSession(maxInactiveIntervalInSeconds = 1200)   // 20분
  ```
- 즉 세션은 **서버 메모리가 아니라 Redis에 저장**된다 → **서버가 여러 대여도 세션 공유** 가능(로드밸런싱 OK), user-web 재시작해도 로그인 유지.
- 코드에서는 `SessionHandler` 로 접근: `session.getAttribute(...)`, `session.isLogin()`
- 우리가 다룬 세션 값 예: `SNS_LOGIN_INFO_DTO`(소셜 로그인 정보), `SNS_EXIST_MEMBER_LIST`(후보 목록), `VIEW_STATUS`(직접접근 방지)

## 장점 / 단점
| 장점 | 단점 |
|--|--|
| 서버가 즉시 **강제 로그아웃** 가능 (세션 지우면 끝) | 요청마다 **세션 저장소 조회** 필요 |
| 사용자에게 정보가 안 나간다(세션ID는 난수) | 쿠키 기반이라 **브라우저 환경**에 묶임 (앱·외부 API 연동엔 불편) |
| 만료·연장 제어가 쉽다 | 서버 여러 대면 **저장소 공유 필요**(그래서 우리는 Redis) |

## ⚠️ 우리가 실제로 당한 함정
소셜 로그인 후보 목록 화면을 **20분 방치** → 세션 만료 → `SNS_LOGIN_INFO_DTO` 소멸 → 그런데 흐름이 계속 진행돼 마지막에 `/userwebundefined` 404.
→ **세션 방식은 "언제든 만료될 수 있다"는 전제**로, 세션을 쓰는 화면·핸들러마다 **없을 때의 처리(가드)** 가 필요하다. (그래서 `snsNewSignup`/`snsNewSignupChk` 에 만료 방어를 넣었다)

---

# 4. 토큰 인증 (access token / refresh token)

## 왜 토큰이 필요한가
세션은 브라우저-서버 1:1 에 잘 맞지만, **모바일 앱 / 외부 서비스(계열사) / 다른 도메인**이 우리 API를 호출할 때는 쿠키·세션이 부적합하다. 그래서 **요청 헤더에 토큰을 실어 보내는 방식**을 쓴다.
```
Authorization: Bearer <access_token>
```

## access token 과 refresh token 을 왜 나누나
| | access token | refresh token |
|--|--|--|
| 용도 | **실제 API 호출**에 사용 | access token **재발급**에만 사용 |
| 수명 | **짧다** (분~시간) | **길다** (일~월) |
| 노출 위험 | 자주 오가서 노출 위험↑ | 거의 안 오가고 보관됨 |

핵심 아이디어: **"자주 쓰는 열쇠는 짧게, 오래 쓰는 열쇠는 깊이 숨긴다"**
- access token 이 탈취돼도 **금방 만료**되니 피해가 제한된다
- 그렇다고 사용자가 매번 재로그인하면 불편 → **refresh token 으로 조용히 재발급**

## 갱신 흐름
```
(1) 로그인 → access(짧음) + refresh(김) 발급
(2) API 호출: Authorization: Bearer <access>
(3) access 만료 → 401 등 오류
(4) refresh 로 토큰 재발급 요청 → 새 access (+ 새 refresh) 수령
(5) 실패한 요청 재시도
(6) refresh 도 만료/무효 → 재로그인 필요
```

## lgoneid 에서 — 토큰이 쓰이는 곳 (두 종류)

### (1) 계열사 연동 토큰 — `TB_MEM_ACC_ID_TOKEN_INFO`
계열사 서비스가 우리(MY LG ID) 회원 정보를 조회할 때 쓰는 토큰.
- 테이블: `TB_MEM_ACC_ID_TOKEN_INFO` (`ACCESS_TOKEN`, `REFRESH_TOKEN`, 만료 관련 컬럼)
- SQL: `member-service/.../sqlmap/MemberAccIdTokenV3_SQL.xml`
  | statement | 역할 |
  |--|--|
  | `retrieveMemberTokenByAffSvcIdSUuid` | 계열사·사용자 기준 **토큰 조회** |
  | `updateMemberTokenInfo` | **토큰 갱신**(재발급 시 저장) — refresh token 으로 조회해 갱신 |
  | `retrieveAccessTokenExpireYn` | access token **만료 여부** 확인 |
  | `retrieveMemberInfoByAffiliates` | access token 으로 **회원정보 조회** |
- 구현: `member-service/.../service/impl/MemberTokenServiceImpl.java`
- 즉 우리는 **토큰 발급자(서버) 역할**을 한다 → 계열사가 access token 으로 API 호출, 만료되면 refresh 로 재발급.

### (2) 소셜 제공자 토큰 — `TB_MEM_SNS`
네이버·카카오가 **우리에게** 발급해 준 토큰 (우리가 소비자 역할).
- `SnsLoginDto.accessToken` / `refreshToken` ← OAuth 콜백에서 수령
- 저장: `TB_MEM_SNS`, 갱신: `updateSnsTokenInfo`
- 용도: 제공자 프로필 API 재조회, 연계 해제 등

### (3) 참고 — JWT 도 쓰인다 (짧은 전달 목적)
- `user-web-service/.../common/service/impl/JwtServiceImpl.java`
  ```java
  long expiredTime = 1000 * 10L;   // 10초
  .claim(DATA_KEY, loginDto)
  ```
- **JWT** = 서명된 토큰(내용 + 만료 + 위조검증 서명). 서버가 저장하지 않아도 검증 가능.
- 여기선 **10초짜리**로 화면·서비스 간 로그인 정보를 **잠깐 안전하게 전달**하는 용도. (장기 인증용이 아님)

---

# 5. 우리 시스템에서 어떻게 공존하나

```
[브라우저 사용자]  ──세션(쿠키+Redis)──> user-web            ← 사람의 웹 로그인
[네이버/카카오]    ──OAuth access/refresh──> user-web        ← 소셜 제공자 토큰 (TB_MEM_SNS)
[계열사 서비스]    ──access/refresh token──> our API          ← 우리가 발급 (TB_MEM_ACC_ID_TOKEN_INFO)
[화면/서비스 간]   ──JWT(10초)──>                             ← 짧은 정보 전달
```
- **사람이 브라우저로 쓰는 로그인** = 세션
- **기계(앱·계열사·외부)가 API 를 쓰는 인증** = 토큰
- 우리 서비스는 **토큰 소비자(소셜)** 이면서 동시에 **토큰 발급자(계열사)** 다

---

# 6. 비교 정리

| 항목 | 쿠키(정보 직접 담기) | 세션 | access token | refresh token |
|--|--|--|--|--|
| 어디에 저장 | 클라이언트 쿠키에 값 자체 | 서버(Redis) + 쿠키엔 ID만 | 클라이언트 보관, 요청 헤더로 전송 | 클라이언트에 안전 보관(거의 전송 안 함) |
| 서버가 상태 기억 | 안 함 | **함** | 안 함(JWT) / 함(DB저장형) | 보통 함(폐기 위해) |
| 수명 | 쿠키 만료(Max-Age) | 20분 무동작(우리 설정) | 짧게 | 길게 |
| 서버 조회 필요 | 불필요 | 매 요청 필요 | (JWT면) 불필요 / (DB저장형이면) 필요 | 재발급 시에만 |
| 위조 위험 | **높음** → 서명 필수 | 낮음(ID는 난수) | 낮음(서명·만료) | 낮음 |
| 강제 무효화 | 어려움 | **쉬움**(세션 삭제) | 어려움(만료 대기/블랙리스트) | 저장형이면 삭제로 가능 |
| 주 사용처 | 비인증 설정값(언어·팝업 등) | 브라우저 웹 화면 로그인 | API 호출 | 재발급 |
| lgoneid | `lang`, `_ga`, `auto-login` 플래그 | **user-web 로그인**(Spring Session+Redis) | 계열사 연동, 소셜 제공자 | 동일 |

> 우리 계열사 토큰은 **DB에 저장하는 방식**(`TB_MEM_ACC_ID_TOKEN_INFO`)이라, 순수 JWT처럼 "서버가 안 기억해도 되는" 형태가 아니라 **조회·만료검사·삭제가 가능한 형태**다. 즉 토큰의 편의성과 세션의 통제력을 절충한 구조.

---

# 7. 실무에서 자주 나오는 질문

**Q. 쿠키 인증과 세션 인증은 뭐가 다른 건가요?**
대립 개념이 아니다. **세션 인증도 쿠키를 사용**한다(쿠키에 세션ID를 담아 전달). "쿠키 인증"이라고 부르는 건 보통 **쿠키에 인증 정보를 직접 담는** 방식이고, 그건 위조 가능해서 **서명(=사실상 토큰)** 이 필요해진다. 정리하면 **쿠키는 전달 수단, 세션·토큰은 신뢰 방식**이다.

**Q. 그럼 토큰도 쿠키에 담을 수 있나요?**
가능하다(`Set-Cookie: token=...; HttpOnly`). 브라우저 서비스에서는 **XSS 로 토큰이 탈취되지 않게 HttpOnly 쿠키에 담는** 방식을 선호하기도 한다. 다만 쿠키는 자동 전송되므로 **CSRF 대비(SameSite·CSRF 토큰)** 가 필요하다. 우리 프로젝트도 POST 시 `_csrf` 토큰을 함께 보낸다(`submitForm`).

**Q. `auto-login` 쿠키가 있으면 그것만으로 로그인되나요?**
아니다. 그건 **"자동로그인 하겠다"는 표시**일 뿐이고, 실제 인증은 `sUuid`·기기 인증 정보와 **서버 검증**(`AutoLoginHandlerImpl`)을 거친다. 인증 판단을 클라이언트 쿠키 값에만 맡기면 안 된다.

**Q. 세션이 있는데 토큰은 왜 또 필요하죠?**
세션은 쿠키·브라우저·도메인에 묶인다. 계열사 서버나 모바일 앱이 우리 API를 호출할 때는 쿠키를 쓸 수 없으니 토큰이 필요하다.

**Q. refresh token 도 탈취되면 위험한데요?**
그래서 (1) 전송을 최소화하고, (2) 서버가 DB에 저장해두고 **필요하면 폐기**할 수 있게 만든다(우리 방식). 재발급 시 refresh 도 새로 주는 방식(rotation)을 쓰면 더 안전하다.

**Q. access token 만료를 어떻게 알아채나요?**
호출해서 401/만료 응답을 받고 재발급하거나, 사전에 만료 여부를 확인한다. 우리 쪽엔 `retrieveAccessTokenExpireYn` 처럼 **만료 여부를 미리 확인하는 조회**가 있다.

**Q. 세션 만료 시 화면은 어떻게 처리해야 하나요?**
세션 값이 없으면 **진행을 멈추고 안내 후 로그인으로 유도**해야 한다. 우리 소셜 신규가입에서 겪은 `/userwebundefined` 404 가 이 처리가 없어서 생긴 사례다.
